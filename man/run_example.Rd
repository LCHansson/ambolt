% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_example.R
\name{run_example}
\alias{run_example}
\title{Run a bundled ambolt example}
\usage{
run_example(name, port = 3000L, run = TRUE)
}
\arguments{
\item{name}{Character. The example slug (e.g. \code{"01-hello"}). Use
\code{list_examples()} to see options.}

\item{port}{Integer. Port to listen on. Default 3000.}

\item{run}{Logical. When TRUE (default), call \code{app$run()} and block on
the running server. When FALSE, return the app object so tests can
inspect it without actually serving.}
}
\value{
When \code{run = FALSE}, the ambolt app object. When \code{run = TRUE},
nothing visible (the call blocks until the server exits).
}
\description{
Discovers example apps shipped in \verb{inst/examples/} and lets you launch
or just construct them. Each example is a self-contained \code{app.R} that
returns an ambolt app object (does not call \code{app$run()} itself when
\code{run = FALSE}, so it can be inspected).
}
\details{
Pattern after Shiny's \code{shiny::runExample}. See \code{ambolt::list_examples()}
for the catalogue.

If an example ships a \code{NOTES.md} next to its \code{app.R}, the contents
are printed to the console at startup. The auth-multipage example
uses this to surface its login credentials (user \code{demo}, password
\code{demo}) before the browser opens.
}
\examples{
\dontrun{
run_example("01-hello")
app <- run_example("01-hello", run = FALSE)
}
}
