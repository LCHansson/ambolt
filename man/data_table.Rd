% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout_dsl.R
\name{data_table}
\alias{data_table}
\title{Interactive data table.}
\usage{
data_table(
  endpoint,
  columns = NULL,
  page_size = 0L,
  searchable = FALSE,
  selectable = FALSE,
  refresh_event = NULL,
  class = NULL,
  style = NULL
)
}
\arguments{
\item{endpoint}{Character. API URL path returning JSON rows.}

\item{columns}{List. Column definitions: \code{list(list(key, label, sortable), ...)}.
If NULL, columns are auto-detected from the first row.}

\item{page_size}{Integer. Rows per page; 0 disables pagination.}

\item{searchable}{Logical. Show a global search input.}

\item{selectable}{Logical. Enable row click selection.}

\item{refresh_event}{Character. Event name that triggers a refetch.}

\item{class}{Character. Additional CSS class(es).}

\item{style}{Named list. Inline CSS properties.}
}
\value{
A layout node (named list with \code{type = "data_table"}).
}
\description{
Modal-context constructor with inline endpoint + columns. Pages continue to
reference tables via bare module-output strings (e.g., "contacts/table").
}
\examples{
data_table(
  endpoint = "/api/contacts",
  columns = list(
    list(key = "name",  label = "Name",  sortable = TRUE),
    list(key = "party", label = "Party", sortable = TRUE)
  ),
  page_size = 25L,
  searchable = TRUE
)
}
