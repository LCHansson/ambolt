% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{create_app}
\alias{create_app}
\title{Create a new ambolt application}
\usage{
create_app(
  port = as.integer(Sys.getenv("AMBOLT_PORT", "3000")),
  host = Sys.getenv("AMBOLT_HOST", "127.0.0.1")
)
}
\arguments{
\item{port}{Integer. Port for the R API server. Default 3000.}

\item{host}{Character. Host address. Default "127.0.0.1".}
}
\value{
An ambolt app object (an environment with registration methods).
}
\description{
Initializes an application object that wraps ambiorix with
framework conventions: CORS middleware, JSON/SVG response helpers,
and a consistent endpoint registration pattern.
}
\details{
Supports two modes:
\enumerate{
\item Stage 1 (explicit): use data_endpoint/plot_endpoint + write your own Svelte
\item Stage 2 (orchestrated): use input/output + app generates everything
}
}
\examples{
\dontrun{
library(ambolt)

app <- create_app(port = 3000L)
app$page("home", title = "Home", ui = page_content(
  page_header(title = "Hello, ambolt!")
))
app$run()
}
}
