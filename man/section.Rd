% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout_dsl.R
\name{section}
\alias{section}
\title{Grouped section with optional label and visibility rules.
Can contain inputs, outputs, or nested layout nodes.}
\usage{
section(
  label = NULL,
  ...,
  show_when = NULL,
  show_after = NULL,
  compact = FALSE,
  toggle = NULL,
  class = NULL,
  style = NULL
)
}
\arguments{
\item{label}{Character (first positional). Optional heading.}

\item{...}{Child item ids or layout nodes.}

\item{show_when}{List. Input-driven visibility condition.}

\item{show_after}{Character. Trigger id for visibility gating.}

\item{compact}{Logical. If TRUE, inputs render in a dense inline layout
(label left-aligned, input right) instead of the default stacked layout.
Useful for assumption/settings panels with many small inputs.}

\item{toggle}{Character. Input id of a checkbox that controls this section.
The checkbox is rendered in the section header (not the body), and
child content is dimmed when unchecked. The checkbox must also appear
in the children list so the framework knows about it.}

\item{class}{Character. Additional CSS class(es).}

\item{style}{Named list. Inline CSS properties (e.g., list(padding = "2rem")).}
}
\value{
A layout node (named list with \code{type = "section"}).
}
\description{
Grouped section with optional label and visibility rules.
Can contain inputs, outputs, or nested layout nodes.
}
\examples{
section("Inputs", "price", "qty", compact = TRUE)
}
